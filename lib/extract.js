/* @flow */

/* Takes a server-side message and converts it to an on-the-
   wire message suitable for sending to server.

   Equivalent to the censor app but cleaner;
   Creates a new object containing only non-sensitive fields,
   leaving the original unchanged.

   To be implemented.
*/

export default message => message;
